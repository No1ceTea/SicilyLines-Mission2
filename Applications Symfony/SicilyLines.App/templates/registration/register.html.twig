{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

        {% block stylesheets %}
        <link href="{{ asset('css/Login.css') }}" rel="stylesheet" type="text/css"/>        
        {% endblock %}
{% block body %}

	<header>
		<div class="navbar">

			<div class="LogoLink">
				<h2>Sicily Lines</h2>
			</div>

			<ul class="NavLink">
				<a class="NavLink" href="{{ path('accueil')}}">Accueil</a>
				<a class="NavLink" href="{{ path('reservation')}}">Réservation</a>
				<a class="NavLink" href="{{ path('contact')}}">Contact</a>
			</ul>

			<ul class="NavBtn">

				<a class="NavLink" href="{{ path('register')}}" style="color:#00aaff ">Inscription</a>
				<a href="{{ path('login')}}"><button id="button">Connexion</button> </a>

			</ul>
	</header>

		<div class="parent">
        

			<div class='div1'>

				<div class="container-LogIn">

                    <h1 style="margin-left:30px; margin-top: 20px;">Inscription</h1>

                    <div class="container" style="margin-left: 30px;">
                    {{ form_start(registrationForm) }}<br>
                        {{ form_row(registrationForm.email) }}<br>
                        {{ form_row(registrationForm.plainPassword, {
                            label: 'Password'
                        }) }}<br>
                        {{ form_row(registrationForm.agreeTerms) }}

                        <button type="submit" class="btn">inscription</button>
                    {{ form_end(registrationForm) }}

                    </div>
                        </form>

                    </div>

                    {% endblock %}